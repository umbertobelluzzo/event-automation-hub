The issue is that node_modules folders aren't actually tracked by Git (which is good!), but the pnpm-lock.yaml file is being tracked and has 10k changes. Here's how to fix this:
Step 1: Update your .gitignore
Your .gitignore needs more entries. Replace the content with:
bash# Dependencies
node_modules/
*/node_modules/
**/node_modules/

# Lock files (optional - you can keep these)
pnpm-lock.yaml
package-lock.json
yarn.lock

# Build outputs
.next/
dist/
build/

# Environment files
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db