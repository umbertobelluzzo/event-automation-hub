{
    "name": "uis-shared",
    "version": "1.0.0",
    "private": true,
    "main": "index.ts",
    "scripts": {
      "build": "echo 'No build step needed for shared types'",
      "type-check": "tsc --noEmit",
      "lint": "echo 'Linting handled by root workspace'"
    },
    "devDependencies": {
      "@types/node": "^20.10.0",
      "typescript": "^5.3.0"
    }
  }